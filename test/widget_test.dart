// Simple placeholder widget test so `flutter test` runs while you review UI
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    // Simple smoke test placeholder so CI/local `flutter test` runs cleanly
    expect(1, equals(1));
  });
}

